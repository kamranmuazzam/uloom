typst was installed using brew
it was tried using cargo, but it didn't install it.

to compile : typst compile example.typ
to watch : typst watch example.typ

for vscode: use tinymist extension, it allows realtime rendering and also debugging